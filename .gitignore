# Secrets — NEVER commit (contains a live You.com API key)
.mcp.json
*.env
.env
.env.*

# Vendored ENGINE (integral to this project): DeepCode — github.com/HKUDS/DeepCode.
# Tracked as source. Its upstream git is neutralized to .git.upstream-HKUDS and
# its virtualenv / runtime lab output are excluded to keep the repo lean.
DeepCode/.git.upstream-HKUDS/
DeepCode/deepcode_lab/
DeepCode/deepcode_hku.egg-info/

# Vendored REFERENCE (kept external): You.com agent starter kit — has its own .git.
youcom-agent-starter-kit/

# Python
__pycache__/
*.pyc
.venv/
venv/

# Node
node_modules/
dist/
build/

# Caches
.cache/
.pytest_cache/
.DS_Store

# Shim scratch dir (temp system prompts for the CLI backend)
copilot_shim/.sandbox/
